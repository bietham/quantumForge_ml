"""01_fetch_and_clean.py

Назначение:
  Скачать страницы LOTR Fandom, извлечь «чистый» текст и сохранить по одному файлу на сущность.

Важно:
  - В этом архиве тексты уже подготовлены, поэтому скрипт оставлен как описание логики (псевдокод).
  - В реальном репозитории можно реализовать скачивание через requests + BeautifulSoup.

Рекомендуемая логика извлечения:
  1) entities = список URL вида https://lotr.fandom.com/wiki/<Entity_Name>
  2) Для каждого url:
     - html = GET(url)
     - soup = BeautifulSoup(html, 'html.parser')
     - main = soup.select_one('main') или soup.select_one('.mw-parser-output')
     - удалить «шум»: навигацию, сайдбары, таблицы, инфобоксы, скрипты/стили, блоки ссылок
     - собрать заголовки + абзацы
     - нормализовать пробелы, убрать дублирующиеся пустые строки
     - сохранить в knowledge_base_raw/<slug>.md:
         # <Entity>
         Source: <url>
         ## Clean text
         <text>

Проверка качества:
  - в файле не должно быть HTML-тегов
  - текст должен читаться как обычная статья, без меню и «сервисных» блоков
"""
